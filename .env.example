APP_HOST=0.0.0.0
APP_PORT=3000
BASE_PREFIX=/

# Optional build-time defaults. The live admin page can override room/socket
# without rebuilding or redeploying GitHub Pages.
VITE_PUBLIC_URL=
VITE_ROOM=planos-faliveis
VITE_SOCKET_ORIGIN=

# Server-only settings are documented in multiplex_client/.env.example.
